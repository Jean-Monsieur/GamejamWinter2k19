using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*Fille qui trip sur les gros muscle*/
public class NPC_1 : MonoBehaviour
{
    private static int BaseStrengh = 5;
    private static int BaseCardio = 2;
    private static int BaseDance = 2;
    private static int BaseIntel = 1;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void coochieGetter(int intel, int cardio, int strenght, int dance)
    {
        //trouver + grande valeur
        int[] tmp = new int[4];

    }




}
